using System;

class Program
{
    public static void Main(string[] args)
    {
        //Sorts three real integers using nested if statements only 
        int[] nums = {3, 5, 1};
        if(num[0] > num[1] && num[0] > num[2])
        {
            Console.WriteLine(num[2] ==)
        }
    }
}